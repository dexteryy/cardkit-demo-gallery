<div class="ck-folder"
        data-enable-label="Hide the {%= state.langName %}"
        data-label="header">
    <header>Show the {%= state.langName %}</header>
    <div class="content democode"><pre class="language-ck"><code class="language-ck">{%= state.code %}</code></pre></div>
</div>
